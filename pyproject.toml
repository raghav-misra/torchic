[project]
name = "torchic-scripts"
version = "0.0.0"
description = "Python tooling for torchic (Kokoro checkpoint conversion)."
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1",
    "safetensors>=0.4",
    "numpy>=1.24",
    "misaki[en]>=0.9",
    "phonemizer-fork>=3.3",
]

[tool.uv]
package = false
